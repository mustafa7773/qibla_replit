// ============================================================================
// إعدادات الموقع
//
// **لا يلزمك كتابة أي شيء هنا للاستخدام العادي.**
//
// كل الأدوات تتصل بقاعدة بيانات الموقع نفسها عبر /api/records في نفس النطاق،
// فتعمل المزامنة بين كل الأجهزة تلقائياً بمجرد فتح الموقع — بلا رابط تلصقه
// ولا مفتاح تكتبه.
//
// كان هنا سابقاً حقل syncEndpoint لرابط Google Apps Script يُكتب في كل جهاز
// على حدة. حُذف: وجود إعداد اختياري بلا فائدة يوهم بأنه مطلوب.
// ============================================================================

window.SkyConfig = {
  // مفتاح مشترك اختياري — للاستخدام العادي اتركه فارغاً.
  //
  // يعمل فقط إن ضبطتَ نفس القيمة في Vercel باسم SKY_API_KEY. وحتى حينها هو
  // تأخير للفضولي لا حماية حقيقية، لأن أي مفتاح في كود المتصفح مكشوف لمن
  // يفتح أدوات المطوّر. الحماية الفعلية الوحيدة:
  // Vercel → Settings → Deployment Protection.
  apiKey: "",
};

// تحقق صريح عند الإقلاع: الفشل الصامت أسوأ من الانهيار.
// كان هذا الملف يحتوي خطأً نحوياً يمنع تحميله كاملاً دون أي إشعار.
(function verifyConfig() {
  if (!window.SkyConfig || typeof window.SkyConfig !== "object") {
    console.error("[Sky] config.js لم يُحمّل — window.SkyConfig غير معرّف.");
  }
})();
